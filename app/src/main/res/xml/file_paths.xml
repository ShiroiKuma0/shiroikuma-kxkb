<?xml version="1.0" encoding="utf-8"?>
<paths>
    <files-path name="error_logs" path="." />
    <!-- Exported layout YAML lives in cacheDir/export/ and is shared via the FileProvider. -->
    <cache-path name="export" path="export/" />
</paths>