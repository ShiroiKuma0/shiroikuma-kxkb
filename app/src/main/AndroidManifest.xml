<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <queries>
        <intent>
            <action android:name="android.view.InputMethod" />
        </intent>
    </queries>

    <application
        android:name=".UrikApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Urik"
        >

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Urik.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/settings_title"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".settings.DictionaryAttributionActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/licenses_title" />

        <activity
            android:name=".settings.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/about_title" />

        <activity
            android:name=".settings.theme.ThemePickerActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/theme_picker_title" />

        <activity
            android:name=".settings.layoutmapper.LayoutMapperActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/layout_mapper_title" />

        <activity
            android:name=".settings.library.KeyboardEditorActivity"
            android:exported="false"
            android:taskAffinity=""
            android:theme="@style/Theme.Urik.NoActionBar" />

        <activity
            android:name=".settings.library.KeyEditActivity"
            android:exported="false"
            android:taskAffinity=""
            android:theme="@style/Theme.Urik.NoActionBar" />

        <!-- Japanese reading→surface word registration. Launched from the ＋登録 affordance in the candidate
             row (FLAG_ACTIVITY_NEW_TASK, own task affinity so it doesn't stack onto Settings). A real Activity
             avoids the focus war the former in-IME attached dialog suffered. (Japanese FIX 2 / BUG B.) -->
        <activity
            android:name=".RegisterWordActivity"
            android:exported="false"
            android:taskAffinity=""
            android:label="@string/ja_register_dialog_title"
            android:theme="@style/Theme.Urik"
            android:windowSoftInputMode="adjustResize|stateVisible" />

        <activity
            android:name=".settings.OssLicensesActivity"
            android:exported="false"
            android:theme="@style/Theme.Urik.NoActionBar"
            android:label="@string/licenses_title" />

        <service
            android:name=".UrikInputMethodService"
            android:exported="true"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|locale|layoutDirection">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Persist the in-app interface-language override (AppCompat per-app locales) below API 33. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>
</manifest>